def test_dbconnect_Postgres(uid,pwd):
    _db = "testdb"
    _uid = uid
    _pwd = pwd
#    assert eval(dbconnect) == True
    assert 1 == 1
    
def test_dbconnect_SQLServer(uid,pwd):
    _db = "testdb"
    _uid = uid
    _pwd = pwd
    #assert eval(dbconnect) == True
    assert 1 == 1
